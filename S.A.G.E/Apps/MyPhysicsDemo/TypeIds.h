#pragma once

#include "SAGE/Inc/SAGE.h"

enum class ComponentId
{
	//CompNameHere = static_cast<uint32_t>(SAGE::ComponentId::Count),
};

enum class ServiceId
{
	//ServiceNameHere = static_cast<uint32_t>(SAGE::ServiceId::Count),
};
